'use client'
import React, { useState, useEffect } from 'react';

// Demo component to show how it works with sections
const ScrollProgress = () => {
  return (
    <div></div>
  );
};

export default ScrollProgress;